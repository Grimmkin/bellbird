enum TextFontFamily {
  sansSerif,
  system,
  monospace,
  round,
  handwriting,
}

enum TextFontWeight {
  regular,
  normal,
  bold,
  italics,
}

enum TextAlignment {
  left,
  right,
  justify,
  center,
}

enum TextType {
  heading1,
  heading2,
  heading3,
  title,
  subtitle,
  body,
  caption,
}

enum Bullet {
  none,
  bullet,
  checkBox,
}
